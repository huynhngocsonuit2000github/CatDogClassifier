# MLflow tracking/registry server (local). In docker-compose this is
# http://mlflow:5000 — the container name on the shared network.
MLFLOW_TRACKING_URI=http://localhost:5000

# Writable dir for the local gate-settings file (see app/gate.py). In
# docker-compose this is /app/data, mounted as a named volume.
DATA_DIR=./data